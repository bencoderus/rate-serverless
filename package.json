{
  "name": "rate-serverless",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Building a serverless rate service with AWS Lambda and API gateway.",
  "dependencies": {
    "axios": "^1.2.1",
    "express": "^4.17.1",
    "response-transformer": "^0.7.1",
    "serverless-http": "^2.7.0"
  },
  "scripts": {
    "start": "serverless offline start",
    "deploy": "yarn test && serverless deploy",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.2.5",
    "jest": "^29.3.1",
    "serverless-offline": "^12.0.3",
    "supertest": "^6.3.3"
  }
}
